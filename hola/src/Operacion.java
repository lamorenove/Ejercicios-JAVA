public class Operacion {

    public int sumar(int a, int b) {
        int resultado = a + b;
        return resultado;
    }
    
Operacion objetoOperacion = new Operacion();
objetoOperacion.sumar(8,7);
System.out.println(numero);
/ Crear
        int valores[];
        valores = new int[3];

        // Asignar valores
        valores[0] = 10;
        valores[1] = 20;
        valores[2] = 30;

        // Recorres arreglo
        for(int i = 0; i < valores.length; i++) {
            System.out.println(valores[i]);
        }
}